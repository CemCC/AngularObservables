import { SearchSelectionsPipe } from './search-selections.pipe';

describe('SearchSelectionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSelectionsPipe();
    expect(pipe).toBeTruthy();
  });
});
